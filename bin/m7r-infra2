#!/bin/bash

__dir="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"

# version=$(cat ${__dir}/../version)
# get first file independent of version number
# file=$(cd ${__dir}/../build/fatjar && ls -1 m7r-infra-single-host-*.jar | head -n 1)

file=$(cd ${__dir}/../build/libs && ls -1 m7r-infra-single-host-*.jar | head -n 1)
# java -jar "${__dir}/../build/fatjar/${file}" "$@"

# java -cp "${__dir}/../build/libs/${file}:${__dir}/../build/dependencies/*" org.mentalizr.infra.InfraCli

echo ">>> ${file}"

java -cp "${__dir}/../build/libs/${file}:${__dir}/../build/dependencies/*" org.mentalizr.infra.InfraCli

